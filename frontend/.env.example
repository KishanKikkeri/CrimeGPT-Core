# API base URL — Vite proxies /api → localhost:8000 in dev.
# For production, set this to the deployed FastAPI URL.
# VITE_API_URL=https://your-crimegpt-api.railway.app

# Demo case ID — the backend seeds this case on startup.
# Change to point at a specific case in your database.
# VITE_DEMO_CASE_ID=CASE-2026-A4F91B
